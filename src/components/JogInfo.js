import React from "react";
import JogsList from "./JogsList";

const JogInfo = () => {
    return (
        <React.Fragment>
            <JogsList/>
        </React.Fragment>
    );
};

export default JogsPage;