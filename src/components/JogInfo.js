import React from "react";
const JogInfo = (props) => {
    return (
        <div>
        <p>{props.distance}</p>
        <p>{props.time}</p>
        <p>{props.date}</p>
        <p>{props.distance}</p>
        </div>
    );
};

export default JogInfo;