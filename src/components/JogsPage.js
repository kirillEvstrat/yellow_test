import React from "react";
import JogsList from "./JogsList";

const JogsPage = () => {
    return (
        <React.Fragment>
            <JogsList/>
        </React.Fragment>
    );
};

export default JogsPage;